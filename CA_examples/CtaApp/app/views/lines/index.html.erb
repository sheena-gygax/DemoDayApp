<h1>Train Lines Go Here</h1>

<p style="color:green;"><%= flash[:notice] %></p>

<%= link_to "Add a New Train Line", new_train_line_url %>

<ul>
<% @lines.each do |line| %>
  <li>
    <%= link_to line.name, train_line_url(line.id) %>
  </li>
<% end %>
</ul>